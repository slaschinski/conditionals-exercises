/**
 * Get the first parameter after the script name of your terminal.
 * Compare this parameter to '-r' and '-f' and output something like:
 * 'You typed -r'
 * If the parameter is neither '-r' nor '-f', output a help text showing
 * which parameters are accepted.
 */
