/**
 * Create a variable with a value of 10.
 * Create an easy comparison that outputs something to the console if the value is 10.
 * Do not output anything if the value is not 10.
 * Try adjusting the variable to see if everything works correctly.
 */
